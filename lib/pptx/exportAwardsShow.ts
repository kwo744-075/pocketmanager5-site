// Minimal PPTX exporter stub — replace with pptxgenjs implementation later.
export async function exportAwardsShow({ year, period_no, themeId, filters }: { year: number; period_no: number; themeId?: string; filters?: any }) {
  // Return a message or buffer in later implementation.
  return { error: "not_implemented" };
}

export default exportAwardsShow;
