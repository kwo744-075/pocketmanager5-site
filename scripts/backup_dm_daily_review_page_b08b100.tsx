/* Backup file intentionally disabled to avoid breaking TypeScript parsing.
   If you need the original backup contents, restore from source control or
   a previous copy. For now this file exports `null` so the build can run. */

export default null;
